services:
  db:
    image: postgres:16
    container_name: openmasjid-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: openmasjid
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d openmasjid"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
